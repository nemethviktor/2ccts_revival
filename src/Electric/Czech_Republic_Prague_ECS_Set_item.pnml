
//  Copyright (C) 2017 Transportman // V. Nemeth 2026+
//  This file is part of the 2cc Trains In NML (Revival) - GNU General Public License v3


// Template: TPL_02C.
// Data from: https://cs.wikipedia.org/wiki/Souprava_metra_E%C4%8Ds

item(FEAT_TRAINS, item_mtro_czech_republic_prague_ecs_set) {
    property {
        name: string(str_mtro_czech_republic_prague_ecs_set);
        climates_available: ((param_region_eastern_europe || 0) && 1) ? ALL_CLIMATES : NO_CLIMATE;
        introduction_date: date(1972,1,1);
        model_life: 45;
        vehicle_life: 60;
        retire_early: 20;
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 120/2 : (param_loadingspeed == 2) ? 120*2 : 120;
        cost_factor: 39.96049;
        running_cost_factor: 36.33454;
        speed: 80 km/h;
        power: 910 hp;
        cargo_capacity: 131;
        weight: 32 ton;
        tractive_effort_coefficient: 0.25;
        air_drag_coefficient: 0;

        reliability_decay: 20;
        // cargodeftype: PASSENGERS_ONLY;
        refittable_cargo_classes: bitmask(CC_PASSENGERS);
        non_refittable_cargo_classes: 0;
        
        cargo_age_period: 185;
        misc_flags: bitmask(TRAIN_FLAG_2CC, TRAIN_FLAG_MU);
        refit_cost: 0;
        ai_special_flag: AI_FLAG_PASSENGER;
        track_type: [STANDARD_GAUGE_RAILTYPE_3RD];
        running_cost_base: RUNNING_COST_ELECTRIC;
        engine_class: ENGINE_CLASS_MAGLEV;
        visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_DISABLE, 0, DISABLE_WAGON_POWER);

        sprite_id: SPRITE_ID_NEW_TRAIN;
        dual_headed: 1;
        length: 8;
        extra_power_per_wagon: 0;
        bitmask_vehicle_info: 0;

	badges: ["region/europe/eastern", "flag/CZ", "power/metro"];

    }
    graphics {
        purchase: spriteset_mtro_czech_republic_prague_ecs_set_purchase;
        // no 'visual_effect' or 'visual_effect_and_powered'
        // no 'length'
        // no 'articulated_part'
        can_attach_wagon: switch_can_attach_vehicle;
        // Add calls to defined switches below
        // RUNNINGCOST_ENGINE_SWITCH_CALL // this is actually blank
        // PURCHASETEXT_SWITCH_CALL // this is actually blank
        default: switch_mtro_czech_republic_prague_ecs_set_reversed;
    }
    livery_override (item_mtro_metro_wagon_unpowered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 120/2 : (param_loadingspeed == 2) ? 120*2 : 120;
        running_cost_factor: int(80/10);
        weight: int(32*1/2);
        cargo_capacity: 150;
        length: 8;
        default: spriteset_mtro_czech_republic_prague_ecs_set_middle;
    }
    livery_override (item_mtro_metro_wagon_powered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 120/2 : (param_loadingspeed == 2) ? 120*2 : 120;
        running_cost_factor: (round(19.141625653031312));
        power: int(910*1/2);
        weight: int(32*3/4);
        cargo_capacity: 150;
        tractive_effort_coefficient: int(0.25*10*32);
        length: 8;
        default: spriteset_mtro_czech_republic_prague_ecs_set_middle;
    }
}
