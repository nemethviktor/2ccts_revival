
//  Copyright (C) 2017 Transportman // V. Nemeth 2026+
//  This file is part of the 2cc Trains In NML (Revival) - GNU General Public License v3


// Template: TPL_02A. Data from: https://en.wikipedia.org/wiki/Silver_City_Comet

// Cargo capacity

    switch(FEAT_TRAINS, SELF, switch_dmu_australia_nswgr_silver_city_comet_capacity_engine, cargo_classes) { \
        bitmask(CC_MAIL): 0/2; \
        bitmask(CC_ARMOURED): 0/4; \
        0; \
    }


    switch(FEAT_TRAINS, SELF, switch_dmu_australia_nswgr_silver_city_comet_capacity_wagon, cargo_classes) { \
        bitmask(CC_MAIL): 60/2; \
        bitmask(CC_ARMOURED): 60/4; \
        60; \
    }

item(FEAT_TRAINS, item_dmu_australia_nswgr_silver_city_comet) {
    property {
        name: string(str_dmu_australia_nswgr_silver_city_comet);
        climates_available: ((param_region_oceania || 0) && 1) ? ALL_CLIMATES : NO_CLIMATE;
        introduction_date: date(1937,1,1);
        model_life: 45;
        vehicle_life: 25;
        retire_early: 20;
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        cost_factor: 29.66511;
        running_cost_factor: 25.95379;
        speed: 160 km/h;
        power: 660 hp;
        cargo_capacity: 0;
        weight: 32 ton;
        tractive_effort_coefficient: 0.135;
        air_drag_coefficient: 0;

        reliability_decay: 20;
        refittable_cargo_classes: bitmask(CC_PASSENGERS, CC_MAIL, CC_ARMOURED);non_refittable_cargo_classes: 0;
        cargo_age_period: 185;
        misc_flags: bitmask(TRAIN_FLAG_2CC, TRAIN_FLAG_MU);
        refit_cost: 0;
        ai_special_flag: AI_FLAG_PASSENGER;
        track_type: [STANDARD_GAUGE_RAILTYPE_NONE];
        running_cost_base: RUNNING_COST_DIESEL;
        engine_class: ENGINE_CLASS_DIESEL;
        visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_DIESEL, 0, DISABLE_WAGON_POWER);

        sprite_id: SPRITE_ID_NEW_TRAIN;
        dual_headed: 1;
        length: 7;
        extra_power_per_wagon: 0;
        bitmask_vehicle_info: 0;

badges: ["power/diesel", "flag/AU"];

    }
    graphics {
        purchase: spriteset_dmu_australia_nswgr_silver_city_comet_purchase;
        cargo_capacity: switch_dmu_australia_nswgr_silver_city_comet_capacity_engine;
        purchase_cargo_capacity: 45;
        visual_effect_and_powered: switch_dmu_australia_nswgr_silver_city_comet_visual_effect_and_powered;
        // no 'length'
        // no 'articulated_part'
        can_attach_wagon: switch_can_attach_vehicle;
        // Add calls to defined switches below
        // RUNNINGCOST_ENGINE_SWITCH_CALL // this is actually blank
        // PURCHASETEXT_SWITCH_CALL // this is actually blank
        default: switch_dmu_australia_nswgr_silver_city_comet_reversed;
    }
    livery_override (item_mu_mu_wagon_unpowered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        running_cost_factor: int(160/10);
        weight: int(32*1/2);
        cargo_capacity: 60;
        length: 7;
        default: switch_dmu_australia_nswgr_silver_city_comet_cargo_selection;
    }
    livery_override (item_mu_mu_wagon_powered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        running_cost_factor: (round(22.889095715520575));
        power: int(660*1/2);
        weight: int(32*3/4);
        cargo_capacity: 60;
        tractive_effort_coefficient: int(0.135*10*32);
        length: 7;
        default: switch_dmu_australia_nswgr_silver_city_comet_cargo_selection;
    }
}
