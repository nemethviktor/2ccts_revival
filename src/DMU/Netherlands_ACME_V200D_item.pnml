
//  Copyright (C) 2017 Transportman // V. Nemeth 2026+
//  This file is part of the 2cc Trains In NML (Revival) - GNU General Public License v3


// Template: TPL_02G.
// Data from: nan

// Notes:
//// WEB: NA -- from Dutch Train set


// Cargo capacity

    switch(FEAT_TRAINS, SELF, switch_dmu_netherlands_acme_v200d_capacity_engine, cargo_classes) { \
        bitmask(CC_MAIL): 60/2; \
        bitmask(CC_ARMOURED): 60/4; \
        60; \
    }


    switch(FEAT_TRAINS, SELF, switch_dmu_netherlands_acme_v200d_capacity_wagon, cargo_classes) { \
        bitmask(CC_MAIL): 60/2; \
        bitmask(CC_ARMOURED): 60/4; \
        60; \
    }

item(FEAT_TRAINS, item_dmu_netherlands_acme_v200d) {
    property {
        name: string(str_dmu_netherlands_acme_v200d);
        climates_available: ((param_region_western_europe || 0) && param_concept) ? ALL_CLIMATES : NO_CLIMATE;
        introduction_date: date(2040,1,1);
        model_life: VEHICLE_NEVER_EXPIRES;
        vehicle_life: 45;
        retire_early: 20;
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        cost_factor: 58.69667;
        running_cost_factor: 48.12787;
        speed: 200 km/h;
        power: 1900 hp;
        cargo_capacity: 60;
        weight: 100 ton;
        tractive_effort_coefficient: 0.2;
        air_drag_coefficient: 0;

        reliability_decay: 20;
        // cargodeftype: PASSENGERS_ONLY;
        refittable_cargo_classes: bitmask(CC_PASSENGERS);
        non_refittable_cargo_classes: 0;
        
        cargo_age_period: 185;
        misc_flags: bitmask(TRAIN_FLAG_2CC, TRAIN_FLAG_MU);
        refit_cost: 0;
        ai_special_flag: AI_FLAG_PASSENGER | AI_FLAG_CARGO;
        track_type: [STANDARD_GAUGE_RAILTYPE_NONE];
        running_cost_base: RUNNING_COST_DIESEL;
        engine_class: ENGINE_CLASS_DIESEL;
        visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_DIESEL, 0, DISABLE_WAGON_POWER);

        sprite_id: SPRITE_ID_NEW_TRAIN;
        dual_headed: 1;
        length: 8;
        extra_power_per_wagon: 0;
        bitmask_vehicle_info: 0;

	badges: ["region/europe/western", "flag/NL", "power/diesel", "role/express_passenger"];

    }
    graphics {
        purchase: spriteset_dmu_netherlands_acme_v200d_purchase;
        cargo_capacity: switch_dmu_netherlands_acme_v200d_capacity_engine;
        visual_effect_and_powered: switch_dmu_netherlands_acme_v200d_visual_effect_and_powered;
        // no 'length'
        // no 'articulated_part'
        can_attach_wagon: switch_can_attach_vehicle;
        // Add calls to defined switches below
        // RUNNINGCOST_ENGINE_SWITCH_CALL // this is actually blank
        // PURCHASETEXT_SWITCH_CALL // this is actually blank
        default: switch_dmu_netherlands_acme_v200d_reversed;
    }
    livery_override (item_mu_mu_wagon_unpowered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        running_cost_factor: int(200/10);
        weight: int(100*1/2);
        cargo_capacity: 60;
        length: 8;
        default: switch_dmu_netherlands_acme_v200d_cargo_selection;
    }
    livery_override (item_mu_mu_wagon_powered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 16/2 : (param_loadingspeed == 2) ? 16*2 : 16;
        running_cost_factor: (round(44.35889900098415));
        power: int(1900*1/2);
        weight: int(100*3/4);
        cargo_capacity: 60;
        tractive_effort_coefficient: int(0.2*10*100);
        length: 8;
        default: switch_dmu_netherlands_acme_v200d_cargo_selection;
    }
}
