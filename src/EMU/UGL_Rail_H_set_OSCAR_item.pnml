
//  Copyright (C) 2017 Transportman // V. Nemeth 2026+
//  This file is part of the 2cc Trains In NML (Revival) - GNU General Public License v3


// Template: TPL_02A.
// Data from: https://en.wikipedia.org/wiki/New_South_Wales_H_set

// Notes:
//// TE_STARTING_KN: 110 -- 110 ea


// Cargo capacity

    switch(FEAT_TRAINS, SELF, switch_emu_ugl_rail_h_set_oscar_capacity_engine, cargo_classes) { \
        bitmask(CC_MAIL): 120/2; \
        bitmask(CC_ARMOURED): 120/4; \
        120; \
    }


    switch(FEAT_TRAINS, SELF, switch_emu_ugl_rail_h_set_oscar_capacity_wagon, cargo_classes) { \
        bitmask(CC_MAIL): 120/2; \
        bitmask(CC_ARMOURED): 120/4; \
        120; \
    }

item(FEAT_TRAINS, item_emu_ugl_rail_h_set_oscar) {
    property {
        name: string(str_emu_ugl_rail_h_set_oscar);
        climates_available: ((param_region_oceania || 0) && 1) ? ALL_CLIMATES : NO_CLIMATE;
        introduction_date: date(2007,1,1);
        model_life: VEHICLE_NEVER_EXPIRES;
        vehicle_life: 45;
        retire_early: 20;
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 40/2 : (param_loadingspeed == 2) ? 40*2 : 40;
        cost_factor: 58.24766;
        running_cost_factor: 52.09159;
        speed: 130 km/h;
        power: 2144 hp;
        cargo_capacity: 120;
        weight: 57 ton;
        tractive_effort_coefficient: 0.197;
        air_drag_coefficient: 0;

        reliability_decay: 20;
        // cargodeftype: PASSENGERS;
        refittable_cargo_classes: bitmask(CC_PASSENGERS, CC_MAIL);
        non_refittable_cargo_classes: 0;
        cargo_age_period: 185;
        misc_flags: bitmask(TRAIN_FLAG_2CC, TRAIN_FLAG_MU);
        refit_cost: 0;
        ai_special_flag: AI_FLAG_PASSENGER;
        track_type: [STANDARD_GAUGE_RAILTYPE_1500V];
        running_cost_base: RUNNING_COST_ELECTRIC;
        engine_class: ENGINE_CLASS_ELECTRIC;
        visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_ELECTRIC, 0, DISABLE_WAGON_POWER);

        sprite_id: SPRITE_ID_NEW_TRAIN;
        dual_headed: 1;
        length: 8;
        extra_power_per_wagon: 0;
        bitmask_vehicle_info: 0;

	badges: ["region/oceania", "power/electric/dc", "role/commuter_urban"];

    }
    graphics {
        purchase: spriteset_emu_ugl_rail_h_set_oscar_purchase;
        cargo_capacity: switch_emu_ugl_rail_h_set_oscar_capacity_engine;
        visual_effect_and_powered: switch_emu_ugl_rail_h_set_oscar_visual_effect_and_powered;
        // no 'length'
        // no 'articulated_part'
        can_attach_wagon: switch_can_attach_vehicle;
        default: switch_emu_ugl_rail_h_set_oscar_reversed;
    }
    livery_override (item_mu_mu_wagon_unpowered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 40/2 : (param_loadingspeed == 2) ? 40*2 : 40;
        running_cost_factor: int(130/10);
        weight: int(57*1/2);
        cargo_capacity: 120;
        length: 8;
        default: switch_emu_ugl_rail_h_set_oscar_cargo_selection;
    }
    livery_override (item_mu_mu_wagon_powered) {
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 40/2 : (param_loadingspeed == 2) ? 40*2 : 40;
        running_cost_factor: round(28.859334761116088);
        power: int(2144*1/2);
        weight: int(57*3/4);
        cargo_capacity: 120;
        tractive_effort_coefficient: int(0.197*10*57);
        length: 8;
        default: switch_emu_ugl_rail_h_set_oscar_cargo_selection;
    }
}
