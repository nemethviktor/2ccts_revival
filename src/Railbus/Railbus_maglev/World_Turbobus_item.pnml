
//  Copyright (C) 2017 Transportman // V. Nemeth 2026+
//  This file is part of the 2cc Trains In NML (Revival) - GNU General Public License v3


// Template: TPL_16B. Data from: nan

// Cargo capacity

    switch(FEAT_TRAINS, SELF, switch_rbm_world_turbobus_capacity_engine, cargo_classes) { \
        bitmask(CC_MAIL): 45/2; \
        bitmask(CC_ARMOURED): 45/4; \
        45; \
    }


    switch(FEAT_TRAINS, SELF, switch_rbm_world_turbobus_capacity_wagon, cargo_classes) { \
        bitmask(CC_MAIL): 0/2; \
        bitmask(CC_ARMOURED): 0/4; \
        0; \
    }

item(FEAT_TRAINS, item_rbm_world_turbobus) {
    property {
        name: string(str_rbm_world_turbobus);
        climates_available: (((param_region_africa || (param_region_north_america || param_region_south_america) || param_region_asia || (param_region_northern_europe || param_region_eastern_europe || param_region_southern_europe || param_region_western_europe) || param_region_oceania) || 0) && param_concept) ? ALL_CLIMATES : NO_CLIMATE;
        introduction_date: date(2040,1,1);
        model_life: VEHICLE_NEVER_EXPIRES;
        vehicle_life: 15;
        retire_early: 20;
        loading_speed: isUltraSpeed ? 255 : (param_loadingspeed == 0) ? 40/2 : (param_loadingspeed == 2) ? 40*2 : 40;
        cost_factor: 79.36745;
        running_cost_factor: 75.26981;
        speed: 753 km/h;
        power: 5070 hp;
        cargo_capacity: 45;
        weight: 30 ton;
        tractive_effort_coefficient: 0.29;
        air_drag_coefficient: 0;

        reliability_decay: 20;
        refittable_cargo_classes: bitmask(CC_PASSENGERS, CC_MAIL, CC_ARMOURED);non_refittable_cargo_classes: 0;
        cargo_age_period: 185;
        misc_flags: bitmask(TRAIN_FLAG_2CC);
        refit_cost: 0;
        ai_special_flag: AI_FLAG_PASSENGER;
        track_type: [MGLV];
        running_cost_base: RUNNING_COST_ELECTRIC;
        engine_class: ENGINE_CLASS_MAGLEV;
        visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_DISABLE, 0, DISABLE_WAGON_POWER);

        sprite_id: SPRITE_ID_NEW_TRAIN;
        dual_headed: 0;
        length: 8;
        extra_power_per_wagon: 0;
        bitmask_vehicle_info: 0;

badges: ["power/maglev", "flag/WRLD"];

    }
    graphics {
        purchase: spriteset_rbm_world_turbobus_purchase;
        cargo_capacity: switch_rbm_world_turbobus_capacity_engine;
        // no 'visual_effect' or 'visual_effect_and_powered'
        length: switch_rbm_world_turbobus_length;
        articulated_part: switch_rbm_world_turbobus_articulated;
        can_attach_wagon: switch_can_attach_vehicle;
        // Add calls to defined switches below
        // RUNNINGCOST_ENGINE_SWITCH_CALL // this is actually blank
        // PURCHASETEXT_SWITCH_CALL // this is actually blank
        default: switch_rbm_world_turbobus_position;
    }
}
